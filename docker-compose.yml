version: '3'

services:
  chat-ui:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - NODE_ENV=production

